<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="onkar.com.duraapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activites.HomeActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".activites.SettingsActivity" />
        <activity android:name=".activites.ProfileActivity" />
        <activity android:name=".activites.HelpActivity" />
        <activity android:name=".activites.ReferActivity" />
        <activity android:name=".activites.GuideActivity" />
        <activity android:name=".activites.AboutActivity" />
        <activity android:name=".activites.LoginActivity" />
        <activity android:name=".activites.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.AdoptionActivity" />
        <activity android:name=".activites.PostDetailActivity"></activity>
    </application>

</manifest>